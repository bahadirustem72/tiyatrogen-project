<%- include('layout', { title: 'Hakkımızda', body: 'hakkimizda_content' }) %>

<% if (false) { %>
    <!-- Boş geçmek için false koşulunu kullanıyoruz -->
    <%- include('hakkimizda_content') %>
<% } %>